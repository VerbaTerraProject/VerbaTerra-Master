# Reproducibility

- Python 3.10–3.11, pinned seeds in simulators.  
- Provide `pyproject.toml` and minimal `requirements`.  
- Tests in `tests/` validate end-to-end run.  
- Notebooks record parameters; avoid hidden state.
